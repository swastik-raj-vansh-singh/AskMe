why shadcn->
it allow to keep our component inside our component folder

anything inside the app folder is route itself 

route grp doesnot effect the url
(foldername)-> just for the structuring not a route (ie != /route)